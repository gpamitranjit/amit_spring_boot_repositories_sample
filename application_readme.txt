import the proejct in SPRING TOOL SUITE/ Eclipse tool
import the 'Spring_repository_custom_methods.postman_collection.json' file in the postman tool
start the spring boot application.
access the following URL: http://localhost:8080/h2-console
click on the 'connect' button
on the console window, execute the queries specified in db_data.txt file.
run the request provided in the imported postman.
try to run the postman request with different searchTerm like 'mytodo' or 'myamit' and observe the response from the application.
done.
